@model Entity02.Models.Company

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>
@using (Html.BeginForm())
{
    @Html.LabelFor(m => m.CompanyName, "Название")
    @Html.EditorFor(m => m.CompanyName)
    @Html.LabelFor(m => m.CompanyLocation, "Расположение")
    @Html.EditorFor(m => m.CompanyLocation)
    <input type="submit" value="Редактировать" />
}

